# Product UI

React + TypeScript (Vite) product card grid. Consumes Product API.

## Run

```powershell
cd project_ui
npm install
npm run dev
```

Set `VITE_API_URL` in an `.env` file to point to the backend when not using the default `http://localhost:3001`.
